const mysql = require('mysql');

const repository = {};

repository.get = (id, data) => {
    // your code...
};

repository.create = (id, data) => {
    // your code...
};

repository.update = (id, data) => {
    // your code...
};

repository.delete = (id, data) => {
    // your code...
};

module.exports = repository;
